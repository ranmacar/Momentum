import $ from './trkl'
import k from './keys'
import ast from './js-ast'

import { persistFile } from "../persist-plugin"
// persistFile('test3333.js', '!!!!!!!')

export default () => {
  const cursor = $({})

  k()
}